{
  "name": "tikfinity-gift-board",
  "version": "1.0.0",
  "private": true,
  "description": "OBS gift board overlay with TikFinity local receiver, admin settings, and Render-ready server.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix local-receiver",
    "start:server": "npm start --prefix server",
    "start:receiver": "npm start --prefix local-receiver"
  }
}
